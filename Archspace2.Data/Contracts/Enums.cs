namespace Archspace2
{
    public enum Command
    {
        Normal,
        Formation,
        Penetrate,
        Flank,
        Reserve,
        Free,
        Assault,
        StandGround
    };

    public enum ListType
    {
        Inclusion,
        Exclusion
    };

    public enum ModifierType
    {
        Absolute,
        Proportional
    };
}
